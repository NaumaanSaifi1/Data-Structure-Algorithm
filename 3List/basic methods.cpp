#include <bits/stdc++.h>
using namespace std;
int main(){

list <int> ls;
ls.push_back(2); // it adds an element to the back of the list  
ls.emplace_back(3); // its faster than push_back
ls.push_front(1); // it makes a copy of the element and then add it to the front
ls.emplace_front(0); // its faster than push_front












}